# python IO basics


# "r" = open the file for reading. which stay in Default mode
# "w" = open file for writing
# "a" = add more content to a file (Append)
# "b" = open the file as a binary mode
# "x" = create file if not exit
# "t" = text mode which stays in default
# "+" = read & write both are working